extends Camera



#func _physics_process(delta):

	#"""var camera = $Camera
	#var from = project_ray_origin(get_viewport().get_mouse_position())
	#var to = from + project_ray_normal(get_viewport().get_mouse_position())
	#player_singleton.target_mouse = to"""
